# @package _global_

defaults:
  - override /hydra: splitter
  - override /splitter: automatic
  - override /data: boli
  
splitter:
  manual_control: False
  seed: [1]

data:
  name: boli_covariates_test
  covariate_key: "predicted.subclass"
  batch_key: "Sample"