# conf/config.yaml

dataset:
  name: replogle_251008_5

adata:
  input_path: /gpfs/home/asun/jin_lab/perturbench/0_datasets/replogle_even_split.h5ad

perturbations:
  randomize: True
  random_seed: 5
  #remove: ["XPO7", "RB1CC1", "SATB2", "CX3CL1", "CUL1", "TBR1"]
  key: gene
  control_value: non-targeting

covariates:
  names: ["cell_line"]

splitter:
  seed: 1
  max_heldout_fraction_per_covariate: 0.6
  max_heldout_covariates: 3

output:
  main_dir: /gpfs/home/asun/jin_lab/perturbench/0_datasets/

manual_control: False

note: |
  Randomly select 6 perturbations to hold out. 
  Test 5 out of 5.
